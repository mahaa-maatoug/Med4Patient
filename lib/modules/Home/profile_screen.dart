import 'package:flutter/material.dart';

class ProfileScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Page Profil", style: TextStyle(fontSize: 20, fontWeight: FontWeight.bold)),
    );
  }
}
